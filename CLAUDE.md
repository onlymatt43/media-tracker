> **Où tu es.** `media-tracker/` est un sous-projet de `metadata/`, lui-même sous
> `~/onlymatt-ecosystem/` — le dossier racine qui regroupe TOUS les projets de Matt. Tu n'es pas au
> sommet. Le répertoire de tout son contenu (l'index `mediaindex`) vit **à la racine** :
> `~/onlymatt-ecosystem/mediaindex.db`. Vue d'ensemble : `~/onlymatt-ecosystem/AGENTS.md`.

@AGENTS.md
